1.github 托管代码：
git add filename                        
git commit -m "注释"
git remote add origin https://github.com/wangyong-hello/yolov8.git
git pull origin master
git push origin master

1.无法 git push:
eval "$(ssh-agent -s)"
ssh-add
